<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hacker-Themed Vulnerability Scanner</title>
    <style>
        /* General Styling */
        body {
            background-color: black;
            font-family: 'Courier New', Courier, monospace;
            color: #00FF00;
            margin: 0;
            padding: 0;
        }

        .terminal {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            padding: 20px;
        }

        .terminal-title {
            font-size: 36px;
            color: #00FF00;
            text-shadow: 0px 0px 5px #00FF00;
        }

        .terminal-description {
            font-size: 18px;
            color: #00FF00;
            text-shadow: 0px 0px 5px #00FF00;
            margin-bottom: 20px;
        }

        .terminal-form {
            width: 100%;
            max-width: 600px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* URL input box styled as a hacker's terminal */
        .terminal-form input[type="text"] {
            width: 100%;
            padding: 15px;
            font-size: 18px;
            border: 2px solid #00FF00;
            background-color: black;
            color: #00FF00;
            text-shadow: 0px 0px 5px #00FF00;
            outline: none;
            box-shadow: 0px 0px 10px #00FF00;
        }

        /* Submit button styled as part of the hacker terminal */
        .terminal-form button {
            margin-top: 20px;
            padding: 10px 30px;
            background-color: black;
            border: 2px solid #00FF00;
            color: #00FF00;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-shadow: 0px 0px 5px #00FF00;
            box-shadow: 0px 0px 10px #00FF00;
        }

        .terminal-form button:hover {
            background-color: #00FF00;
            color: black;
            text-shadow: none;
        }

        /* Glitch animation */
        @keyframes glitch {
            0% {
                transform: translate(0);
            }
            20% {
                transform: translate(-2px, 2px);
            }
            40% {
                transform: translate(2px, -2px);
            }
            60% {
                transform: translate(-2px, -2px);
            }
            80% {
                transform: translate(2px, 2px);
            }
            100% {
                transform: translate(0);
            }
        }

        /* Add glitch effect to title */
        .terminal-title {
            animation: glitch 1.5s infinite;
        }
    </style>
</head>
<body>

    <div class="terminal">
        <h1 class="terminal-title">[Vulnerability Scanner]</h1>
        <p class="terminal-description">Enter the URL below to scan for vulnerabilities:</p>
        
        <!-- Hacker-style form for URL input -->
        <form class="terminal-form" action="/scan" method="POST">
            <input type="text" name="url" id="url" placeholder="http://example.com" required>
            <button type="submit">Scan Website</button>
        </form>
    </div>

</body>
</html>
